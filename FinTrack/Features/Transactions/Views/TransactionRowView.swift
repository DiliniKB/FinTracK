import SwiftUI

struct TransactionRowView: View {
    let transaction: Transaction

    var body: some View {
        // TODO: Implement — colored icon circle, category name + payee, LKR amount
        // (green for income, red for expense), note subtitle if non-empty
        EmptyView()
    }
}
