import SwiftUI

struct TransactionsScreen: View {
    // TODO: Inject via environment or init
    var body: some View {
        // TODO: Implement — monthly summary card, filter bar, grouped transaction list
        // with swipe-to-delete, tap-to-edit, + toolbar button, and empty state
        Text("Transactions")
    }
}

#Preview {
    TransactionsScreen()
}
