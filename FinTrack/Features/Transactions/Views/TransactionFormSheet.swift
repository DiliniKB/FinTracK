import SwiftUI

struct TransactionFormSheet: View {
    // TODO: Inject TransactionViewModel
    // TODO: Inject available categories list

    var body: some View {
        // TODO: Implement — amount field (LKR, numeric keyboard), type toggle,
        // category picker, date picker, payee field, note field, Save/Cancel
        EmptyView()
    }
}

#Preview {
    TransactionFormSheet()
}
